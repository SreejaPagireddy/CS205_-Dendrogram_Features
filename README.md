This project explores feature selection and clustering methods for machine learning datasets. 

We used forward selection and backward elimination to find useful feature subsets for a 1-nearest neighbor classifier using leave-one-out cross-validation. 

The algorithms are tested on synthetic datasets and the UCI Wine dataset. 


The project also includes data preprocessing, feature normalization, result visualization, and hierarchical clustering with dendrograms to better understand the structure of the data.
